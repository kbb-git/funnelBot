services:
  - type: web
    name: funnelbot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn run:app
    envVars:
      - key: GEMINI_API_KEY
        sync: false